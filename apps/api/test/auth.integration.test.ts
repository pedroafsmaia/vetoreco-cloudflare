import { describe, it } from 'vitest';

describe('auth/protected routes integration (scaffold)', () => {
  it.skip('deve validar login + cookie + rota protegida com ambiente D1 local (wrangler/miniflare)', async () => {
    // Scaffold intentionally skipped in repository template.
    // Rodar localmente com ambiente D1 e adaptar para app.request(...) / integração real.
  });
});
